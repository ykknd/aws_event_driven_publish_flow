[project]
name = "publish-flow-engine"
version = "0.1.0"
description = "Dummy Python execution engine for publish_flow"
requires-python = ">=3.11"
dependencies = [
  "boto3>=1.38.0",
  "ipykernel>=6.29.5",
  "nbclient>=0.10.2",
  "nbformat>=5.10.4",
  "python-pptx>=1.0.2",
]

[dependency-groups]
dev = [
  "pytest>=8.3.5",
]

[tool.uv]
package = false
